import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  icons: {
    margin: '8px',
  },
  button: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export { useStyles };
