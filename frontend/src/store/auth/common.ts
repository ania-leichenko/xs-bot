enum ActionType {
  SIGN_UP = 'auth/sign-up',
  SIGN_IN_MASTER = 'auth/sign-in-master',
  SIGN_IN_WORKER = 'auth/sign-in-worker',
  LOAD_CURRENT_USER = 'auth/load-current-user',
  LOG_OUT = 'auth/log-out',
}

export { ActionType };
